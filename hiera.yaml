---
:backends: 
 - yaml
:logger: console
:hierarchy:
 - "common"

:yaml:
# NOTE: FOR DEVELOPERS, THIS PATH MUST BE /etc/puppet/hiera.
# # NOT THE DEFAULT VALUE --> /etc/puppet/hiera/%{environment}
# # that is used by actual nodes
:datadir: "/etc/puppet/hiera/"
